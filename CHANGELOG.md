# Журнал версий и замечаний — «Опись пачки»

Формат: пока актуальна версия vN, новые замечания к ней нумеруются N.1, N.2, N.3...
Как только они реализованы и вышла версия vN+1 — раздел закрывается пометкой
«реализовано в vN+1», и дальше уже идёт (N+1).1, (N+1).2 для следующего раунда.

Правило по релизам: копим замечания здесь, ничего не внедряем сразу по ходу
разговора. Новую версию делаю только после явного «да» от Сергея.

Принцип для широкой раскладки (iPad): цель — не просто рабочий интерфейс, а
чтобы приложение ощущалось родным и одинаковым на любом устройстве. Все элементы,
которые есть и на iPhone, и на iPad (шрифты, отступы, размеры кнопок и блоков),
должны совпадать пиксель в пиксель с версией на iPhone везде, где это возможно.
Разница между устройствами оправдана только в том, что реально зависит от ширины
экрана (ширина списка, боковая панель вместо шторки снизу) — не в мелких деталях
вроде размера одной кнопки.

---

## Ожидают реализации

- **67.1** — Название и комментарий позиции — переделать в две вкладки одного и
  того же места на карточке, а не два отдельных блока подряд:
  - Вкладки называются "Название" и "Комментарий", расположены сверху самого
    поля — в том пустом пространстве, что сейчас есть между заголовком
    "Позиция №N" и текстовым полем.
  - Значок микрофона остаётся на том же самом месте визуально, но надиктовывает
    в то поле, чья вкладка сейчас открыта (то название, то комментарий) — не
    отдельные кнопки, одна и та же.
  - Кнопка "наговорить заметку" и сами звуковые заметки (клипы) переезжают
    внутрь вкладки "Комментарий" целиком — не отдельный блок под всей
    карточкой, как сейчас, а часть именно этой вкладки, ниже текста комментария.
  - У списка заметок внутри вкладки — своя внутренняя прокрутка. Сколько бы
    заметок ни накопилось, сама карточка позиции в размере не растёт вообще —
    сейчас список заметок раздувает всю карточку, чем их больше, тем хуже
    выглядит, это и есть то, что чинит эта правка.

    сейчас список заметок раздувает всю карточку, чем их больше, тем хуже
    выглядит, это и есть то, что чинит эта правка.

- **67.2** — Комментарий к пачке целиком (не к позиции) — те же две проблемы:
  - БАГ: сейчас открывается по "+", но закрыть обратно нечем — нет минусика или
    любого другого способа свернуть поле назад. Нужно добавить.
  - Кнопка "наговорить заметку" и сами звуковые заметки пачки — переезжают
    внутрь самого поля комментария пачки, со своей прокруткой, по тому же
    принципу, что и у позиции (67.1) — не раздувают блок под фото пачки, сколько
    бы заметок ни накопилось.
  - БАГ: кнопка микрофона в поле комментария пачки визуально сидит не на месте,
    будто не помещается в поле — причина в том, что у самого поля добавлены
    отступы слева/справа, а кнопка микрофона позиционируется без их учёта,
    отсчитывая от другого края. Нужно привести к одной системе координат.

---

## v67 (текущая, фронтенд + скрипт)

- **66.1** — Перенос позиции между пачками: кнопка в карточке, выбор пачки-получателя
  через список "Все пачки", подтверждение, новый номер в пачке-получателе, фото/аудио
  физически переезжают и переименовываются. Журнал переносов — новый лист
  "moved_positions" (время, откуда, куда, текст, старые имена фото текстом, новые —
  кликабельными ссылками).
- **66.2** — Комментарий к позиции и к пачке (свёрнут по умолчанию, с голосовым
  вводом текста) и звуковые заметки (несколько клипов, копятся, в приложении только
  звук). На сервере заметки распознаются в отдельный лист "заметки_текст" — для
  поиска, не для показа в приложении. Ссылка на заметку кликабельна и там, и в
  основной Таблице (колонка "аудио").

## v66 (фронтенд)

- Список "Все пачки" теперь сортируется по возрастанию номера (сначала младшие,
  потом старшие), раньше порядок был по убыванию — и трудно ориентироваться.

## v65 (скрипт, обновлён повторно — тот же номер, фронт не менялся)

- При пересохранении позиции с уже существующими фото — если настоящее имя файла
  на Диске не соответствует принятому виду (осталось от более ранней версии
  приложения), файл теперь реально переименовывается на Диске, а не просто
  отображается как есть.
- Диагностика: если для какого-то фото не удаётся прочитать имя файла или
  переименование не удалось — записывается в лог ошибок на GitHub.

## v65 (текущая, фронтенд + скрипт)

- БАГ починен: удаление позиции целиком теперь реально доходит до сервера — раньше
  такого запроса не было вообще, кнопка "Удалить позицию" убирала строку только
  локально в браузере, в Таблице она оставалась и возвращалась обратно при
  следующей синхронизации. Теперь строка переезжает целиком в новый архивный лист
  "deleted_positions" (выглядит так же, как в обычном списке), фото и аудио
  физически переезжают в папку "Удалённые фото". Отправка идёт через ту же
  надёжную очередь повторных попыток, что и обычное сохранение.
- Имя файла фото теперь работает как постоянный идентификатор — номер никогда не
  переиспользуется заново, даже после удаления фото.
- Отдельно удалённые фото (не вся позиция, а конкретное фото) тоже переносятся в
  "Удалённые фото" — та же папка, тот же принцип.

## v64 (фронтенд + скрипт)

- **63.1** — БАГ починен: фоновая синхронизация больше не останавливается просто
  от того, что открыта карточка позиции. Защищаются только реально несохранённые
  текст/количество (сверка со снимком из 62.2) — фото обновляются в открытой
  карточке всегда живьём, свежее фото с другого устройства теперь появляется само,
  без переоткрытия.
- **63.2** — Подтверждение перед удалением фото (и у позиции, и у пачки целиком) —
  "Удалить это фото? Да / Отмена", как уже есть для удаления позиции.
- Заодно найденный и починенный баг: удаление фото пачки целиком раньше вообще не
  доходило до сервера — только пряталось локально в браузере, в Таблице ссылка
  оставалась и возвращалась после следующей синхронизации. Теперь удаление реально
  уходит на сервер (нужно обновление скрипта).

## v63 (фронтенд)

- **62.1** — Геометрия стрелочки-указателя исправлена — теперь целиком в зазоре
  между списком и панелью, не задевает карточку.
- **62.2** — БАГ починен: автосохранение при переключении между позициями больше
  не срабатывает, если реально ничего не изменилось — сравнивается с исходным
  состоянием карточки на момент открытия. Двойное дёргание списка при простом
  перелистывании должно уйти.

## v62 (фронтенд)

- **61.1** — Выделение активной позиции доработано: цвет рамки/стрелочки — тот же
  серо-стальной, что у номера позиции (был слишком тёмный). Рамка убрана совсем —
  "выпуклость" теперь через реальный рост самой карточки наружу (влево и вправо),
  а не через внутренние отступы (номер больше не сдвигается). Стрелочка крупнее
  в 1.5 раза.

## v61 (фронтенд)

- **60.1** — Выделение активной позиции (59.1) усилено — заметнее тень и контраст.
- **60.2** — Треугольник-стрелочка (59.1) крупнее и теперь в самом зазоре между
  списком и панелью, не прилеплен к краю карточки.
- **60.3** — Починена подсветка активной строки, пропадавшая при фоновой
  синхронизации — теперь сверяется по видимому номеру позиции, а не по id.
- **60.4** — Фоновое обновление больше не перерисовывает список без нужды —
  сравнивает свежие данные с уже показанными, экран не дёргается зря каждые
  10-15 секунд, если реально ничего не изменилось.

## v60 (фронтенд)

- **59.1** — На iPad активная позиция в списке (та, что сейчас открыта в панели
  справа) визуально подсвечивается: тень, чуть больше отступы, треугольник-стрелочка
  с правого края (первая версия — доработана в v61).

## v59 (фронтенд)

- **58.1** — QR-блоки (позиции и пачки) перенесены в тот же ряд, что превьюшки
  фото, на место кнопки камеры, точного размера с ней.
- **58.2** — Убран поясняющий текст под списком позиций.
- **58.3** — БАГ из v58 починен: кнопка "+" на iPad снова крепится к экрану.
- **58.4** — БАГ из v58 починен: волна-эквалайзер центрирована и шире на 50%.
- **58.5** — Строка "Количество" + счётчик прижаты к правому краю карточки.

## v58 (фронтенд)

- Список слева — ровно 430px (ширина iPhone 14 Pro Max), карточка справа
  заполняет всё оставшееся место без пустого зазора.
- QR-код на пачку целиком, список "Все пачки" над левой колонкой.
- Иконка корзинки с линией, подпись "Количество" сближена со счётчиком.
- Счётчик количества и кнопка микрофона — крупнее в 1.5 раза.

## v57 (фронтенд)

- Пропорции широкой раскладки наоборот: карточка ~2/3 экрана, список ~1/3.
- Автосохранение на широкой раскладке (iPad) при переключении позиции/пачки.
- Кнопка "+" привязана к правому краю списка, а не экрана.

## v56 (фронтенд)

- Широкая раскладка для iPad/компьютера: карточка позиции — отдельная панель справа.
- Случайный зум страницы при быстрых тапах — отключено на уровне всей страницы.

## v55 (фронтенд)

- QR-код: крупнее зерно (короткие ASCII-параметры вместо кириллицы в ссылке).
- QR-код перенесён с верха карточки в ряд с фото — на место кнопки камеры.

## v54 (скрипт, фронтенд)

Крупное: реальное распознавание голоса и сохранение аудио в Таблицу, фото-превью
не зависят от готовности ссылок Google Диска, кнопка микрофона и волна-эквалайзер
внутри поля ввода, печатная форма по пачке и архив всей описи (2 PDF) через меню
в самой Таблице, QR-код и фоновая синхронизация для связки iPad↔iPhone, камера
только на телефонах, лог ошибок в GitHub.

Плюс серия исправлений одного и того же корневого бага «сохранение позиции с фото
откатывается назад»: гонка с фоновой синхронизацией, поиск позиции по неверному
служебному id, устаревшие записи в очереди неотправленных сохранений, и настоящая
причина — сервер пытался заново «загрузить» уже загруженные фото и падал с ошибкой,
из-за которой не сохранялось вообще ничего.
