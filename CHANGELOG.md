# Журнал версий и замечаний — «Опись пачки»

Формат: пока актуальна версия vN, новые замечания к ней нумеруются N.1, N.2, N.3...
Как только они реализованы и вышла версия vN+1 — раздел закрывается пометкой
«реализовано в vN+1», и дальше уже идёт (N+1).1, (N+1).2 для следующего раунда.

Правило по релизам: копим замечания здесь, ничего не внедряем сразу по ходу
разговора. Новую версию делаю только после явного «да» от Сергея.

Принцип для широкой раскладки (iPad): цель — не просто рабочий интерфейс, а
чтобы приложение ощущалось родным и одинаковым на любом устройстве. Все элементы,
которые есть и на iPhone, и на iPad (шрифты, отступы, размеры кнопок и блоков),
должны совпадать пиксель в пиксель везде, где это возможно при разной ширине
экрана. Разница между устройствами — только там, где она оправдана содержанием
(ширина списка, положение боковой панели), а не в мелких деталях вроде размера
одной конкретной кнопки.

---

## Ожидают реализации

- **58.1** — QR-код пачки целиком сейчас сидит в строке заголовка "Фото пачки
  целиком", оторван от самих превьюшек фото. Перенести в тот же ряд, что и
  превьюшки — на место кнопки камеры. Важно: блок QR-кода должен быть ТОЧНО того
  же размера, что кнопка камеры, которую заменяет (52×52px), а не 64×64px, как
  сейчас — на вид ни отступы, ни размер не должны отличаться от iPhone. Касается
  и QR-кода позиции (уже в релизе), и QR-кода пачки.
- **58.2** — Убрать поясняющий текст под списком позиций ("Голос распознаётся
  по-настоящему...") — сейчас уже интуитивно понятно, подсказка не нужна.
- **58.3** — Кнопка "+" (новая позиция) сейчас плавающая, всегда прижата к низу
  экрана — из-за этого визуально далеко от последней карточки списка. Сделать
  обычным элементом сразу под последней позицией, а не плавающей кнопкой.
  Компромисс: при длинном списке кнопку придётся долистывать до конца, а не она
  всегда под рукой, как сейчас — Сергей об этом предупреждён, хочет именно так.

---

## v58 (текущая, фронтенд)

- **57.1** — Список слева теперь ровно 430px (ширина iPhone 14 Pro Max), карточка
  справа заполняет всё оставшееся место без пустого зазора (кроме небольшого,
  ~24px, между ними).
- **57.2** — QR-код на пачку целиком, рядом с "Фото пачки целиком" — ведёт не на
  позицию, а просто на саму пачку. Виден только на планшете/компьютере.
- **57.3** — Список "Все пачки" на широкой раскладке теперь выезжает снизу над
  левой колонкой (той же ширины, что список позиций), а не по центру экрана.
- **57.4** — Иконка корзинки — добавлена одна вертикальная линия внутри, ближе к
  стандартной иконке iOS.
- **57.5** — Подпись "Количество" сближена со счётчиком (было растянуто на всю
  ширину строки).
- **57.6** — Счётчик количества (минус/поле/плюс) — крупнее в 1.5 раза.
- **57.7** — Кнопка микрофона в поле названия — крупнее в 1.5 раза.

## v57 (фронтенд)

- Пропорции широкой раскладки наоборот: карточка ~2/3 экрана, список ~1/3
  (пропорции позже уточнены в v58 под точную ширину iPhone).
- Автосохранение на широкой раскладке (iPad): переключение позиции/пачки/новая
  позиция при открытой карточке — несохранённые правки сохраняются сами.
- Кнопка "+" на широкой раскладке привязана к правому краю списка, а не экрана.

## v56 (фронтенд)

- Широкая раскладка для iPad/компьютера: карточка позиции — отдельная панель справа.
- Случайный зум страницы при быстрых тапах — отключено на уровне всей страницы.

## v55 (фронтенд)

- QR-код: крупнее зерно (короткие ASCII-параметры вместо кириллицы в ссылке).
- QR-код перенесён с верха карточки в ряд с фото — на место кнопки камеры.

## v54 (скрипт, фронтенд)

Крупное: реальное распознавание голоса и сохранение аудио в Таблицу, фото-превью
не зависят от готовности ссылок Google Диска, кнопка микрофона и волна-эквалайзер
внутри поля ввода, печатная форма по пачке и архив всей описи (2 PDF) через меню
в самой Таблице, QR-код и фоновая синхронизация для связки iPad↔iPhone, камера
только на телефонах, лог ошибок в GitHub.

Плюс серия исправлений одного и того же корневого бага «сохранение позиции с фото
откатывается назад»: гонка с фоновой синхронизацией, поиск позиции по неверному
служебному id, устаревшие записи в очереди неотправленных сохранений, и настоящая
причина — сервер пытался заново «загрузить» уже загруженные фото и падал с ошибкой,
из-за которой не сохранялось вообще ничего.
